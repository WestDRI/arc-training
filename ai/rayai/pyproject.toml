[project]
name = "rayai"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "ray[data,serve,train,tune]>=2.52.0",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
    "jupyter-cache>=1.0.1",
]
